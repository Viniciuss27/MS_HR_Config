spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/db_hr_worker  #porta 5433
    username: postgres
    password: 1234567

  jpa:
    hibernate:
      ddl-auto: create #create para gerar as tabelas automaticamente
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
